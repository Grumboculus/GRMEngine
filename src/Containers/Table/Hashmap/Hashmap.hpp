#ifndef HASHMAP_HPP
#define HASHMAP_HPP

namespace engine {
namespace data_structs {

    /**
     * @brief A basic Hashmap data structure.
     * 
     * @tparam K The key type used for hashing.
     * @tparam V The value type mapped to by the keys.
     */
    template <typename K, typename V>
    class Hashmap {
        public:
            /**
             * @brief Default constructor for Hashmap.
             */
            Hashmap() {}

            /**
             * @brief Destructor for Hashmap.
             */
            ~Hashmap() {}

            // Implement insert, remove, retrieve, and hashing operations here.
    };

}
}

#endif
