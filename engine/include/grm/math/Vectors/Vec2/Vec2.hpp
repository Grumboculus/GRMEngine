#ifndef VEC2_HPP
#define VEC2_HPP

template <typename T>

class Vec2 {
    private:
        T x, y;
    public: 

        Vec2(const T xVal, const T yVal) void {
            x = xVal;
            y = yVal;

            return;
        }

        GetX() const T {
            return x;
        }

        GetY() const T {
            return y;
        }

        xy() const T {
            return x, y;
        }

        SetX()
};

#endif